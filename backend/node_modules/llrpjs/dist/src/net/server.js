"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.LLRPServerOfDef = void 0;
const net_1 = __importDefault(require("net"));
const typed_1 = require("./typed");
const LLRPServerOfDef = (Def) => class LLRPServer extends typed_1.LLRPTypedNet.ofDef(Def) {
    constructor(options) {
        super(options);
        const server = net_1.default.createServer();
        this.initializeServer(server);
    }
    listen() {
        this._server.listen(this.options.port, this.options.host);
    }
    async close() {
        await this.cleanupSocket();
        return new Promise((resolve, reject) => {
            if (this._server) {
                this._server.close(err => {
                    if (err)
                        reject(err);
                    else
                        resolve();
                });
            }
            else {
                resolve();
            }
        });
    }
};
exports.LLRPServerOfDef = LLRPServerOfDef;
//# sourceMappingURL=server.js.map